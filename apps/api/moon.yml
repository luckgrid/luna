language: python
layer: application
stack: backend

fileGroups:
  configs:
    - "*.toml"
  sources:
    - "src/**/*"

project:
  name: api
  description: FastAPI + Pydantic AI backend for AI features.

tasks:
  setup:
    command: "uv"
    args: ["sync"]
    inputs:
      - "@group(configs)"
    outputs:
      - "uv.lock"
    options:
      runFromWorkspaceRoot: true

  dev:
    command: "uv"
    args:
      - "run"
      - "--with"
      - "pydantic-ai"
      - "--with"
      - "fastapi"
      - "--with"
      - "uvicorn"
      - "--with"
      - "pydantic"
      - "uvicorn"
      - "src.main:app"
      - "--reload"
      - "--port"
      - "8080"
    env:
      PYTHONPATH: "src"
    options:
      persistent: true

  start:
    command: "uv"
    args:
      - "run"
      - "--with"
      - "pydantic-ai"
      - "--with"
      - "fastapi"
      - "--with"
      - "uvicorn"
      - "--with"
      - "pydantic"
      - "python"
      - "src/main.py"
    options:
      persistent: true
