language: typescript
layer: library
stack: "frontend"

tags:
  - bun-ts-lib

project:
  name: "ds"
  description: "Luna design-system styles and CSS-first tokens."

workspace:
  inheritedTasks:
    include:
      - "clean"
      - "typecheck"
