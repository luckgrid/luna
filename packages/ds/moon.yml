language: typescript
layer: library
stack: "frontend"

tags:
  - bun

project:
  name: "ds"
  description: "Luna design-system styles and CSS-first tokens."

workspace:
  inheritedTasks:
    include:
      - "clean"
      - "typecheck"
